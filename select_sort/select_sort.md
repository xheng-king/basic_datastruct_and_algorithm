### 大体实现
（与冒泡排序对比阅读）  
约定：数组有效数据位从0到length-1  

1.外层循环：i从0到length-2(i从第一个元素到倒数第二个元素，即数据个数-1趟排序)  
2.设置min为i  
3.内层循环：j从i+1到length-1(j从i号的下一个元素到最后一个元素)，用于寻找i+1到length-1的最小元素下标赋给min  
4.交换min和i位置处的元素  

冒泡与选择极为相似，都是n-1趟排序，内层也都是i号的下一个元素到最后一个元素，只不过方向相反  