## 大体实现 
约定：0号位作为哨兵，数组长度length是指数组实际物理长度为length+1，即1~length为有效数据位  

1.外层循环：i从2到length(i从第二个元素到最后一个元素)  
2.对比i处元素与i-1处元素的大小  
　　2.1若i处元素大于等于i-1处元素，则跳过该轮循环  
　　2.2否则，置哨兵为i号元素，开启内层循环：j从i-1递减，直到j处元素小于或等于哨兵，每次循环置j+1处元素为j处元素  
　　2.3出循环后j当前的位置是小于等于哨兵的下标，将哨兵放在j+1处  